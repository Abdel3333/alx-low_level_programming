#include <ctype.h>
/**
 * _islower - check if a character is lowercase
 * @c: The character to check
 * Return: 0 or 1
 */
int _islower(int c)
{
	if (islower(c) != 0)
		return (1);
	else
		return (0);
}
