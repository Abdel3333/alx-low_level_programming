#include "main.h"
/**
 * *_memcpy - copy memory data
 * @dest: pointer destimation
 * @src: pointer source
 * @n: number of bytes
 * Return: a pointer
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		dest[i] = src[i];
	}
	return (dest);
}
